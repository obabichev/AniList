'use strict';

import React, {Component, PropTypes} from 'react';
import {View, Text, Image} from 'react-native';

export default class AnimeItemCard extends Component {

    constructor(props){
        super(props);
    }

    render(){
        return (
            <View>

            </View>
        );
    }
};

AnimeListView.propTypes = {
    animeItem: React.PropTypes.object
};